# HANDOFF — состояние работы. Только актуальное

Правило файла: держим только то, что верно СЕЙЧАС. Закрыл пункт — удали его,
история живёт в `git log`.

## Сейчас

Репозиторий стал одночастным: 29.08.2026 механика поиска работы уехала в
`~/Projects/kadr` (приватный, `git@github.com:jorqen/kadr.git`). Здесь остались
`resume/resume.yaml`, генератор выгрузок, сайт и его оснастка. Правила —
`AGENTS.md`.

Проверено после отделения: `scripts/build_resume_formats.sh` собирается,
`.venv/bin/python -m scripts.run_tests` зелёный (1 модуль, 17 тестов), pyright —
ноль ошибок.

## Остаток

1. **`.venv` здесь тяжелее, чем нужно.** В нём стоят зависимости уехавшего
   сборщика (playwright, telethon, trafilatura, imap-tools). Генератору нужны
   только babel, jinja2, jsonschema, python-docx, pillow, pyyaml, weasyprint.
   Пересобрать окружение — отдельная задача, ломать рабочее без просьбы не стал.
2. **`.idea/` в `.gitignore`, но каталог остался** от прежней раскладки: в нём
   ссылки на модули уехавшего пакета. На сборку не влияет.

## Как работать

1. Правка резюме — только в `resume/resume.yaml`; собранные файлы руками не трогать.
2. Собрал → прогнал гейт ATS → коммит. Не собралось — правки нет.
3. Коммиты атомарные и поимённо; `git add -A` не использовать.
